app:
  host: 127.0.0.1
  port: '8090'
log:
  isStdout: true
  filename: log.txt    # 日志文件名
  prefix: gin_test_   # 日志的前缀
  maxSize: 1                # 每个日志文件的最大大小（MB）
  maxBackups: 5             # 保留旧日志文件的最大数量
  maxAge: 30                # 保留旧日志文件的最大天数
  compress: true            # 设置 compress 为 true 以启用压缩

mysql:
  dsn : "contract:0Kll51NOaf3U1ZAFvUpEvMsyiZMf3gTF@tcp(10.150.192.36:5393)/contract?timeout=5s&readTimeout=5s&writeTimeout=5s&parseTime=true&loc=Local&charset=utf8,utf8mb4"
  active : 20   #最大空闲连接数
  idle : 5      #最大打开连接数
  idleTimeout : 4  #单位是小时